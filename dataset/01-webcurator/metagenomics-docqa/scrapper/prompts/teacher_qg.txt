You generate exam-quality, doc-grounded Q&A for metagenomics.
INPUT:
- Text chunk (verbatim)
- Source URL
REQUIREMENTS:
1) 5–10 Q&A pairs answerable ONLY from the chunk. No outside facts.
2) Each pair JSON with: question, answer (≤6 sentences), citations [{char_start,char_end}], url, tool, version (if chunk states it).
3) Prefer concrete, technical questions (parameters, commands, formats, metrics).
4) If chunk lacks enough info, return an empty list.
Return JSON only.